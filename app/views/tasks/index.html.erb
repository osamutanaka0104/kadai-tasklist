<h1>タスク一覧</h1>
<p>
  現在、ログインしているユーザー: <%= @current_user.name %>
  <%= link_to "ログアウト", logout_path, method: :delete,data: {confirm: "ログアウトしますか?"} %>
</p>
<ul>
    <% @tasks.each do |task| %>
      <li><%=link_to task.id , task %> : <%= task.content %> &gt; <%= task.status %></li>
    <% end %>
</ul>

<%= link_to "新しいタスクを追加する", new_task_path %>